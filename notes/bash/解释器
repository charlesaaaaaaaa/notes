当写一个bash文件的时候如果没有声明解释器的位置，则需要在运行该脚本的前面要加对应的解释器
如运行一个sh文件，一般如果没有在脚本第一行声明，则在运行的时候要bash file.sh
如果在脚本的第一行声明了 #!/bin/bash 则运行该脚本可以直接./file.sh

但有一些脚本第一行声明用的是 #!/usr/bin/env bash
这个写法就是说让env去$PATH里面找bash，因为有的linux发行版bash不一定放在/bin目录下面

